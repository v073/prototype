db:
    file:           db
    schema_file:    schema.sql

token_length: 9

voting:
    max_token_count: 100
    default_options:
        pro_contra:
            - Dafür
            - Dagegen
            - Enthaltung
